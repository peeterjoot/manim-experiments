from helper import *

class TitlePage( Scene ):
    def construct( self ):
        t = Text( "Geometric algebra: projection and rejection." )
        t.shift( 3 * DOWN )

        # something here.
        self.wait( 1 )


# vim: et sw=4 ts=4
